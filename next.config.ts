import type { NextConfig } from "next";

// Next.js 16 no longer runs ESLint during `next build` — lint runs separately
// via `npm run lint`. No eslint-ignore flag needed anymore.
const nextConfig: NextConfig = {};

export default nextConfig;
