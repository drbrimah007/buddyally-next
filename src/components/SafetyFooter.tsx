// Intentionally left empty. SafetyFooter (the global slim strip above the
// bottom nav) was rolled back per user request — Safety/Report should
// appear inline at the bottom of the Groups page (and Activities/Messages),
// matching the existing SafetyBanner pattern, not as a persistent footer.
//
// File kept (not deleted) because the sandbox can't `rm` it; safe to
// `git rm src/components/SafetyFooter.tsx` from your terminal whenever.
export {}
